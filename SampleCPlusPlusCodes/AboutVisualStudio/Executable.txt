Note that the executable file of the project is built inside the debug folder.
The executable file is portable, meaning because it's not a library, it has everything it needs inside.  So just move
the file around and execute it, it is the machine code!